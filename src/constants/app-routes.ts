export const AppRoutes = {
    ROOT: '/',
    LOGIN: '/login',
    REGISTER: '/register',
    PROFILE: '/profile',
    GROUPS: '/groups',
    CALLBACK: '/callback',
    AUTH_ERROR: '/auth-error',
} as const;
